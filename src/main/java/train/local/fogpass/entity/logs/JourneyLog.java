package train.local.fogpass.entity.logs; // Note the sub-package

import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.Table;

/**
 * A minimal JourneyLog entity that creates a database table
 * named 'journey_log' with only a primary key column.
 */
@Entity
@Table(name = "journey_log") // Explicitly naming the table
public class JourneyLog {

    @Id // Marks this field as the primary key.
    @GeneratedValue(strategy = GenerationType.IDENTITY) // Configures the ID to be auto-generated by the database.
    private Long id;

    // --- Getters and Setters ---

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }
}