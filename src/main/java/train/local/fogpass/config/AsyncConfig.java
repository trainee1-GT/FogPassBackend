package train.local.fogpass.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AsyncConfig {
    // Async configuration can be added here if needed
    // The bulkUploadExecutor has been moved to BulkUploadConfig to avoid duplicate bean definition
}